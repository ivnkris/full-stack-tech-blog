{{#if posts.isLoggedIn}}
{{> privateNavbar }}
{{else}}
{{> publicNavbar }}
{{/if}}
<article>
    {{#if posts.isEmpty}}
        {{> emptyHomepage}}
    {{else}}
    {{#each posts as |post| }}
        {{> postInfo post}}
    {{/each}}
    {{/if}}
</article>